mutation SaveActivityReply(
    $id: Int
    $activityId: Int!
    $text: String!
) {
    SaveActivityReply(activityId: $activityId, text: $text) {
        id
        text
        activityId
        createdAt
        likeCount
        isLiked
    }
}
