mutation ToggleThreadSubscription(
    $threadId: Int!
    $subscribe: Boolean!
) {
    ToggleThreadSubscription(
        threadId: $threadId
        subscribe: $subscribe
    ) {
        id
        title
        isSubscribed
    }
}
